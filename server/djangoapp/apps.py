from django.apps import AppConfig
# configuration class that represents this 'djangoapp' app and appears/ installed/ registered in the setting.py file of site (django project) folder/ container.


class DjangoappConfig(AppConfig):
    name = 'djangoapp'
